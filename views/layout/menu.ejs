<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if(session.username){ %>
      <li class="nav-item">
      <a class="nav-link">Selamat datang <%= session.username%></a>
      </li>
      <% } else {%>
        <li class="nav-item">
        <a class="nav-link" href="/login">Login</a>
        </li>
        <li class="nav-item">
        <a class="nav-link" href="/createUser">Create User</a>
        </li>
      <%}%>

      <% if(session.role === `teacher` || session.role === `headmaster`) {%>
      <li class="nav-item">
        <a class="nav-link" href="/teacher">Teacher<span class="sr-only">(current)</span></a>
      </li>
      <% } %>
      <% if(session.role === `teacher` || session.role === `headmaster`) {%>
      <li class="nav-item">
        <a class="nav-link" href="/subject">Subject</a>
      </li>
      <% } %>
      <% if(session.role === `academic` || session.role === `headmaster`) {%>
      <li class="nav-item">
        <a class="nav-link" href="/student">Student</a>
      </li>
      <% } %>
      <% if(session.username){ %>
        <li class="nav-item">
          <a class="nav-link" href="/logout">Logout</a>
        </li>
      <% } %>
    </ul>
  </div>
</nav>
